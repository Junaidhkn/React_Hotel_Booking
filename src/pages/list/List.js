import './list.css';
import Navbar from '../../components/navbar/Navbar';
import Header from '../../components/header/Header';

const List = () => {
	return (
		<div>
			<Navbar type='list' />
			<Header type='list' />
		</div>
	);
};

export default List;
